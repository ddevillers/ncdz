import { ParachutisteValidePipe } from './parachutiste-valide.pipe';

describe('ParachutisteValidePipe', () => {
  it('create an instance', () => {
    const pipe = new ParachutisteValidePipe();
    expect(pipe).toBeTruthy();
  });
});
